import React from 'react';
import ReactDOM from 'react-dom';
import MeasurementConverter from './App';

ReactDOM.render(
  <React.StrictMode>
    <MeasurementConverter />
  </React.StrictMode>,
  document.getElementById('root')
);
